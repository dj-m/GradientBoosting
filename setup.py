from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='The application of gradient boosting to  a regression problem, then to a classification problem using the Titanic dataset.',
    author='Domingo Moronta',
    license='MIT',
)
